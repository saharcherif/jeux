
  # final version (Copy)

  This is a code bundle for final version (Copy). The original project is available at https://www.figma.com/design/kov6cOHelYNxoSbI917CN2/final-version--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  